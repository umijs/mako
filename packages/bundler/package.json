{
  "name": "@utoo/bundler",
  "version": "0.0.0-alpha.1",
  "main": "cjs/index.js",
  "module": "esm/index.js",
  "bin": "./bin/cli.js",
  "files": [
    "./cjs/*.js",
    "./cjs/*.d.ts",
    "./esm/*.js",
    "./esm/*.d.ts",
    "./bin/cli.js",
    "./global.d.ts"
  ],
  "napi": {
    "name": "bundler",
    "triples": {
      "additional": [
        "i686-pc-windows-msvc",
        "aarch64-unknown-linux-gnu",
        "aarch64-apple-darwin",
        "x86_64-unknown-linux-musl",
        "aarch64-unknown-linux-musl",
        "aarch64-pc-windows-msvc"
      ]
    }
  },
  "license": "MIT",
  "dependencies": {
    "nanoid": "^3.3.11",
    "picocolors": "^1.1.1",
    "@babel/code-frame": "7.22.5"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0",
    "@types/node": "^20.3.0",
    "@types/babel__code-frame": "7.0.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">= 10"
  },
  "scripts": {
    "build:cjs": "rm -rf cjs && tsc -p ./tsconfig.json --module commonjs --outDir cjs && cp src/*.d.ts cjs/",
    "build:esm": "rm -rf esm && tsc -p ./tsconfig.json --module esnext --outDir esm && cp src/*.d.ts esm/",
    "artifacts": "napi artifacts --dir ./src --dist npm",
    "build:binding": "napi build src --platform --release -p bundler-napi --cargo-cwd ../../ --cargo-name bundler_napi --features plugin --js binding.js --dts binding.d.ts --strip",
    "build:binding:debug": "napi build src --platform -p bundler-napi --cargo-cwd ../../ --cargo-name bundler_napi --features plugin --js binding.js --dts binding.d.ts",
    "prepublishOnly": "npm run build:esm && npm run build:cjs && napi prepublish -t npm",
    "version": "napi version"
  }
}

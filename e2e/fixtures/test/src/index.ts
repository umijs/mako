// const requireAll = (context) => context.keys().map(context);
// requireAll((require as any).context('./models', true, /\.tsx$/));

import './test2';
console.log('aaaaa');

// import './foo';
// delete item.fromTarget.splice(key, 1);;

# utoo

## Quickly Start

## Build from source
```bash
cargo build --release
export PATH=$PATH:$(pwd)/target/release
```

### Install Deps

```bash
utoo
```

### Run Dev Server
```bash
utoo-bundler-cli dev --dir examples/with-antd --root . --target browser
```

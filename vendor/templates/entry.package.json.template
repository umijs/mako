{
  "name": "@utoo/{{name}}",
  "version": "{{version}}",
  "description": "Utoo CLI tool - {{name}}",
  "bin": {
    "{{name}}": "bin/{{name}}"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/umijs/mako.git"
  },
  "os": ["darwin", "linux"],
  "cpu": ["x64", "arm64"],
  "optionalDependencies": {
    "@utoo/{{name}}-linux-x64": "{{version}}",
    "@utoo/{{name}}-darwin-x64": "{{version}}",
    "@utoo/{{name}}-darwin-arm64": "{{version}}"
  },
  "scripts": {
    "postinstall": "bash ./postinstall.sh"
  }
}
